#	filter = [ "r|/dev/zd*|" ]
sed -i '/# This configuration option has an automatic default value\./,+1 {/^[[:space:]]*# filter = \[ "a|\.\*|" \]$/a\        filter = [ "r|/dev/zd*|" ]
}' /etc/lvm/lvm.conf
